//CRIA PILHA VAZIA

pilha *p = cria_pilha();

{
pilha *p = malloc (sizeof(pilha);  //aloca espaco para a pilha
p->topo=NULL;                      // testa pilha vazia
return p;                          // retorna valor
}


// CRIA TIPO PILHA

typedef struct fila {             // cria struc para fila
no *inicio;
no *fim;            
}fila;

// INSERIR NA FILA


//CRIA FILA VAZIA
pilha *p = malloc (sizeof(pilha); 
p->inicio=NULL
p->fim=NULL


/////////
typedef struct fila {
struct fila *inicio;
struct fila *fim;
}fila;

typedef struct no{    
int valor;
struct no *proximo;
}no;

malloc (sizeof(fila);


if {
fila -> inicio ==NULL}
fila -> inicio = no;

no-> proximo = NULL;
if {
fila -> fim != NULL}
fila ->fim->proximo=no;
fila -> fim = no;

  
  
  
